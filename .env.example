# Server
PORT=5000
# Local dev base URL; on Render set to your service URL (e.g., https://luvra-backend.onrender.com)
APP_BASE_URL=http://localhost:5000
# Deep link scheme for the Expo app (use luvra://); if you host a web client, change accordingly
FRONTEND_URL=luvra://

# Database
MONGO_URI=your-mongodb-connection-string

# Auth
# Use a long random string
JWT_SECRET=your-jwt-secret

# SMTP (choose one provider and fill values)
# -- Gmail (requires 2FA + App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Luvra <your@gmail.com>"

# -- Mailtrap (testing)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=your_mailtrap_user
# SMTP_PASS=your_mailtrap_pass
# SMTP_FROM="Luvra <no-reply@luvra.test>"

# Cloudinary (for avatar & chat media uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
